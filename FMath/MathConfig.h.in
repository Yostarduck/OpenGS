#define FMATH_VERSION_MAJOR @FMATH_VERSION_MAJOR@
#define FMATH_VERSION_MINOR @FMATH_VERSION_MINOR@

#cmakedefine BUILDING_FMATH_SHARED

#define USE_CUSTOM_MATH 0
#define USE_STD_MATH 1

#define MATH_IMPLEMENTATION USE_CUSTOM_MATH

#ifdef _WIN32
    #ifdef BUILDING_FMATH_SHARED
        #define FMATH_API __declspec(dllexport)  // Export when building
    #else
        #define FMATH_API __declspec(dllimport)  // Import when using
    #endif
#else
    #define FMATH_API  // Empty for non-Windows platforms
#endif